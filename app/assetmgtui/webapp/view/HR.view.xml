<!-- <mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        <FlexBox justifyContent="Center" height="100%">
            <Button text="Create Request" press="createHRRequest" />
        </FlexBox>
    </Page>
</mvc:View> -->
				


<!-- 
<mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        <List items="{hrRequestsModel>/}">
            <StandardListItem title="{hrRequestsModel>product}" />
            <CustomListItem>
                <FlexBox>
                    <Text text="Status:" />
                    <Text text="{hrRequestsModel>status}" />
                </FlexBox>
            </CustomListItem>
            
        </List>
    </Page>
</mvc:View> -->

<!-- 
<mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        
            <Button text="Create Request" press="createHRRequest" />
       
        <List items="{hrRequestsModel>/}">
            <StandardListItem title="{hrRequestsModel>product}" />
            <CustomListItem>
                <FlexBox>
                    <Text text="Status:" />
                    <Text text="{hrRequestsModel>status}" />
                </FlexBox>
            </CustomListItem>
            
        </List>
    </Page>
</mvc:View>
 -->


 
<!-- <mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true" xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}" showNavButton="true" navButtonPress="onNavBack">

        <headerContent>
            <Button id="addEmployeeButton" icon="sap-icon://add-employee" tooltip="Add Employee" press="onAddEmp" />
        </headerContent>
        <List
            id="EmployeesList"
            class="sapUiResponsiveMargin"
            width="auto"
            growing="true"
            growingThreshold="5"
            items="{
                path: 'hrRequestsModel>/'
            }">

            <headerToolbar>
                <Toolbar>
                    <Label id="tableHeader" text="Ticket Request List"/>
                    <ToolbarSpacer />
                    <SearchField id="searchField" width="auto" search="onSearchEmployee" />
                </Toolbar>
            </headerToolbar>
            <items>
                <ObjectListItem
                    title="{hrRequestsModel>product}"
                    type="Navigation"
                    press="onSelect">
                    <ObjectAttribute text="{hrRequestsModel>hrRequestID}" />
                    <ObjectAttribute text="{hrRequestsModel>requestCreationDate}" />
                </ObjectListItem>
            </items>
        </List>
    </Page>
</mvc:View> -->



<!-- 
<mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        <content>
            
                <Button text="Create Request" press="createHRRequest" />
            
            <Table items="{hrRequestsModel>/}">
            <columns>
                <Column>
                    <header>
                        <Label text="Request Generation Date"></Label>
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="Request ID"></Label>
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="Item"></Label>
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="Status"></Label>
                    </header>
                </Column>
            </columns>
            <items>
                <ColumnListItem vAlign="Inherit">
                    <cells>
                        <Text text="{hrRequestsModel>requestCreationDate}">
                        </Text>

                        <Text text="{hrRequestsModel>hrRequestID}">
                        </Text>

                        <Text text="{hrRequestsModel>product}">
                        </Text>

                        <Text text="{hrRequestsModel>status}">
                        </Text>

                    </cells>
                </ColumnListItem>
            </items>
            </Table>
        </content>
    </Page>
</mvc:View>  -->



<!-- 
<mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        <headerContent>
            <Button id="createHrRequestButton" icon="sap-icon://create-form" tooltip="Create Request" press="onCreateHrRequest" />
        </headerContent>
        <ToolbarSpacer/>
        <ToolbarSpacer/>
        <content>
             <Button text="Create Request" press="createHRRequest" /> 
				
            <Table items="{hrRequestsModel>/}" class="hover-table" headerText="RequestList">
                  <headerToolbar>
        <Toolbar>
            <Title text="RequestList" textAlign="Center"/>
        </Toolbar>
    </headerToolbar> 
                <columns>
                    <Column>
                        <header>
                            <Label text="Request Generation Date"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Request ID"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Item"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Status"></Label>
                        </header>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem vAlign="Inherit">
                        <cells>
                            <Text text="{hrRequestsModel>requestCreationDate}"/>
                            <Text text="{hrRequestsModel>hrRequestID}"/>
                            <Text text="{hrRequestsModel>item}"/>
                              <ObjectStatus
                                text="{hrRequestsModel>status}"
                                state="{= ${hrRequestsModel>status} === 'Success' ? 'Success' : ${hrRequestsModel>status} === 'Cancel' ? 'Error' : 'Warning' }" />

                             <ObjectStatus
                                class="sapUiSmallMarginBottom"
                                text="{hrRequestsModel>status} - from absisek"
                                state="{path: 'hrRequestsModel>status',
                                        formatter: '.formatter.statusText'}" /> 



                          <Text text="{hrRequestsModel>status}"  /> 

                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View> -->



<mvc:View controllerName="assetmgtui.controller.HR"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>hrTitle}">
        <headerContent>
            <Button id="createHrRequestButton" icon="sap-icon://create-form" tooltip="Create Request" press="onCreateHrRequest" />
        </headerContent>
        <ToolbarSpacer/>
        <ToolbarSpacer/>
        <content>
            <!-- <Button text="Create Request" press="createHRRequest" /> -->
				
            <Table items="{hrRequestsModel>/}" class="hover-table" headerText="RequestList">
                  <!-- <headerToolbar>
        <Toolbar>
            <Title text="RequestList" textAlign="Center"/>
        </Toolbar>
    </headerToolbar> -->
                <columns>
                    <Column>
                        <header>
                            <Label text="Request Generation Date"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Request ID"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Item"></Label>
                        </header>
                    </Column>
                    <Column>
                        <header>
                            <Label text="Status"></Label>
                        </header>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem vAlign="Inherit">
                        <cells>
                            <Text text="{hrRequestsModel>requestCreationDate}"/>
                            <Text text="{hrRequestsModel>hrRequestID}"/>
                            <Text text="{hrRequestsModel>item}"/>
                              <ObjectStatus
                                text="{hrRequestsModel>status}"
                                state="{= ${hrRequestsModel>status} === 'Success' ? 'Success' : ${hrRequestsModel>status} === 'Cancel' ? 'Error' : 'Warning' }" />


                                <!-- <ObjectStatus
    text="{hrRequestsModel>status}"
    state="{
        parts: [{path: 'hrRequestsModel>status'}],
        formatter: '.formatter.getStatusState'
    }" /> -->

                            <!-- <ObjectStatus
                                class="sapUiSmallMarginBottom"
                                text="{hrRequestsModel>status} - from absisek"
                                state="{path: 'hrRequestsModel>status',
                                        formatter: '.formatter.statusText'}" /> -->



                           <!-- <Text text="{hrRequestsModel>status}"  /> -->

                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
        <footer>
            <Bar design="Footer">
                <contentRight>
                    <Button text="Route to TL1" press="handleNavigation"></Button>
                </contentRight>
            </Bar>
        </footer>
    </Page>
</mvc:View>

